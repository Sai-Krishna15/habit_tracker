import { Controller } from "@hotwired/stimulus";

export default class extends Controller {
  updateStats(event) {
    // The form will be submitted automatically
    // After the form submission, the stats will be updated via Turbo Frame
  }
}
