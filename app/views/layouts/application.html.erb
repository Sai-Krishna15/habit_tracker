<!DOCTYPE html>
<html>
  <head>
    <title>Consistently - Habit Tracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-white shadow-lg">
      <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to "Consistently", root_path, class: "text-xl font-bold text-indigo-600" %>
            </div>
          </div>
          <div class="flex items-center">
            <% if user_signed_in? %>
              <div class="ml-4 flex items-center space-x-4">
                <%= link_to "My Habits", habits_path, class: "text-gray-700 hover:text-indigo-600" %>
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-gray-700 hover:text-indigo-600" %>
              </div>
            <% else %>
              <div class="ml-4 flex items-center space-x-4">
                <%= link_to "Sign In", new_user_session_path, class: "text-gray-700 hover:text-indigo-600" %>
                <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-700 hover:text-indigo-600" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-4 py-8">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
