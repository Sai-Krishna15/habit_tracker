<%= form_with(model: habit, data: { turbo: false }, class: "space-y-6") do |form| %>
  <div>
    <%= form.label :name, class: "block text-sm font-medium text-gray-700" do %>
      Habit Name <span class="text-red-500">*</span>
    <% end %>
    <div class="mt-1">
      <%= form.text_field :name, required: true,
          class: "appearance-none block w-full px-3 py-2 border #{habit.errors[:name].any? ? 'border-red-300' : 'border-gray-300'} rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm",
          placeholder: "e.g., Morning Exercise" %>
      <% if habit.errors[:name].any? %>
        <p class="mt-2 text-sm text-red-600"><%= habit.errors[:name].join(", ") %></p>
      <% end %>
    </div>
  </div>

  <div>
    <%= form.label :description, class: "block text-sm font-medium text-gray-700" do %>
      Description <span class="text-red-500">*</span>
    <% end %>
    <div class="mt-1">
      <%= form.text_area :description, required: true, rows: 3,
          class: "appearance-none block w-full px-3 py-2 border #{habit.errors[:description].any? ? 'border-red-300' : 'border-gray-300'} rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm",
          placeholder: "Describe your habit and why it's important to you" %>
      <% if habit.errors[:description].any? %>
        <p class="mt-2 text-sm text-red-600"><%= habit.errors[:description].join(", ") %></p>
      <% end %>
    </div>
  </div>

  <div class="flex items-center justify-between pt-4">
    <%= link_to "Cancel", habits_path, class: "inline-flex justify-center py-2 px-4 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
     <%= form.button type: "submit",
      class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500",
      onclick: "this.disabled=true; this.innerHTML='<svg class=\"animate-spin -ml-1 mr-3 h-5 w-5 text-white inline\" xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" viewBox=\"0 0 24 24\"><circle class=\"opacity-25\" cx=\"12\" cy=\"12\" r=\"10\" stroke=\"currentColor\" stroke-width=\"4\"></circle><path class=\"opacity-75\" fill=\"currentColor\" d=\"M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z\"></path></svg> Saving...'; this.form.submit();" do %>
    <%= habit.new_record? ? 'Create Habit' : 'Update Habit' %>
  <% end %>
  </div>
<% end %>